# OS clutter
Thumbs.db
desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Local editor / tooling config — never part of the published site
.claude/

# Original, uncropped photographs — keep these out of git history.
# Crop them into photos/<person-id>.jpg and commit that instead.
photos/originals/
photos/*.HEIC
photos/*.heic
